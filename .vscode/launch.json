{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Image Filter",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/image",
            "args": ["pic.jpg", "blur"],
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "miDebuggerPath": "gdb",
            "preLaunchTask": "build C project",
        }
    ]
}
